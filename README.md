# containerimage_aurora_43_with_nix

**DEPRECATED**

I am no longer keeping this up to date, as I switched back to the official
images combined with transient root and creation of `/nix/` by a tmpfiles file.

Container image for U-Blue Aurora 43 with a `/nix/` directory.
